{% extends 'base.html.twig' %}

{% block title %}StockUnique{% endblock %}

{% block body %}
    <h1>StockUnique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_unique.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ stock_unique.quantity }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ stock_unique.totalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_unique_index') }}">back to list</a>

    <a href="{{ path('stock_unique_edit', {'id': stock_unique.id}) }}">edit</a>

    {{ include('stock_unique/_delete_form.html.twig') }}
{% endblock %}
